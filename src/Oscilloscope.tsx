import React from 'react';

/**
 * Componente del osciloscopio (UI Mock).
 * Este es un componente básico que representa una maqueta del UI.
 */
const OscilloscopeUI = () => {
    return (
        <div>
            <h2>Oscilloscope UI</h2>
            <p>Este es el diseño básico de la interfaz del osciloscopio.</p>
        </div>
    );
};

export default OscilloscopeUI;
